[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "varneb"
version = "0.0.2"
description = "VARiable-cell Nudged Elastic Band code with universal first-frinciples calculators"
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-3.0-or-later"
authors = [{name = "VARNEB developers"}]
dependencies = [
  "ase>=3.22",
  "numpy>=1.22",
]

[project.urls]
Homepage = "https://github.com/xdzhu/varneb"
Repository = "https://github.com/xdzhu/varneb"
Issues = "https://github.com/xdzhu/varneb/issues"

[project.optional-dependencies]
plot = ["matplotlib>=3.6"]
dev = ["pytest>=7", "ruff>=0.4"]

[project.scripts]
varneb = "vcneb.cli:main"
vcneb = "vcneb.cli:main"

[tool.setuptools.packages.find]
include = ["vcneb*"]
